<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>عيد ميلاد أغلى فرح</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/three.js/r128/three.min.js"></script>
    <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;700;900&family=Amiri:ital,wght@0,400;0,700;1,400&display=swap" rel="stylesheet">
    <style>
        body { margin: 0; overflow-x: hidden; background: #fff0f3; font-family: 'Cairo', sans-serif; }
        #bg-canvas { position: fixed; top: 0; left: 0; width: 100%; height: 100%; z-index: -1; }
        .glass-panel { background: rgba(255, 255, 255, 0.94); backdrop-filter: blur(12px); border: 1.5px solid #ffb3c1; border-radius: 30px; box-shadow: 0 15px 45px rgba(255, 77, 109, 0.15); }
        .hidden-content { display: none; }
        .balloon { position: absolute; animation: float 7s ease-in-out infinite; pointer-events: none; z-index: 50; }
        @keyframes float { 0%, 100% { transform: translateY(0) rotate(0deg); } 50% { transform: translateY(-40px) rotate(5deg); } }
        .btn-celebrate { background: linear-gradient(45deg, #ff4d6d, #ff758f); transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275); }
        .btn-celebrate:hover { transform: scale(1.08) translateY(-3px); box-shadow: 0 10px 25px rgba(255, 77, 109, 0.4); }
        .message-title { font-family: 'Amiri', serif; color: #c9184a; }
        .heart-beat { animation: heartbeat 1.5s ease-in-out infinite; }
        @keyframes heartbeat { 0% { transform: scale(1); } 14% { transform: scale(1.1); } 28% { transform: scale(1); } 42% { transform: scale(1.1); } 70% { transform: scale(1); } }
        .fade-in { animation: fadeIn 2s ease-out; }
        @keyframes fadeIn { from { opacity: 0; transform: translateY(20px); } to { opacity: 1; transform: translateY(0); } }
    </style>
</head>
<body>
    <canvas id="bg-canvas"></canvas>

    <!-- واجهة الدخول -->
    <div id="login-section" class="min-h-screen flex items-center justify-center p-4">
        <div class="glass-panel p-10 max-w-md w-full text-center fade-in">
            <div class="text-6xl mb-6">🎁</div>
            <h1 class="text-3xl font-black text-pink-600 mb-6">أهلاً بكِ يا أميرة</h1>
            <p class="text-gray-600 mb-8 font-semibold">المكان مخصص فقط لـ "فرح"<br>الرجاء إدخال كلمة السر السرية</p>
            <input type="password" id="password" placeholder="كلمة السر..." class="w-full p-4 rounded-2xl border-2 border-pink-100 mb-6 text-center focus:border-pink-500 outline-none transition-all shadow-inner">
            <button onclick="checkPassword()" class="btn-celebrate text-white font-bold py-4 px-10 rounded-full w-full text-xl shadow-lg">افتحي الهدية ✨</button>
            <p id="error-msg" class="text-red-500 mt-4 hidden font-bold">كلمة السر غير صحيحة يا فرح</p>
        </div>
    </div>

    <!-- واجهة الاحتفال الرئيسية -->
    <div id="celebration-section" class="hidden-content min-h-screen p-6 md:p-12">
        <div class="max-w-4xl mx-auto space-y-10 pb-32 text-center">
            
            <!-- العنوان الكبير -->
            <div class="glass-panel p-10 border-pink-300 relative overflow-hidden">
                <div class="absolute -top-10 -right-10 text-8xl opacity-10 rotate-12">🎂</div>
                <h2 class="text-4xl md:text-6xl font-black text-red-600 leading-tight fade-in">
                    عيد ميلاد سعيد يا نجمة العائلة <br>
                    <span class="text-pink-500">"فرح الغالية"</span>
                </h2>
                <div class="flex justify-center gap-6 text-4xl mt-8 animate-bounce">
                    <span>🎈</span><span>✨</span><span>🍰</span><span>🌸</span><span>💖</span>
                </div>
            </div>

            <!-- الرسالة المحدثة: نص طويل وجميل -->
            <div class="glass-panel p-10 text-right leading-relaxed shadow-xl border-r-8 border-pink-500">
                <h3 class="message-title text-3xl font-bold mb-6 text-pink-700">في يوم ميلادكِ الـ ١٩..</h3>
                <div class="space-y-6 text-xl text-gray-800 leading-loose">
                    <p>
                        اليوم لا نحتفل فقط بمرور عام آخر من عمرك، بل نحتفل بوجودك الذي يمنح حياتنا معنىً ولوناً. فرح، أنتِ لستِ مجرد اسم، بل أنتِ السعادة التي تدخل قلوبنا دون استئذان.
                    </p>
                    <p>
                        تسعة عشر عاماً مضت، كبرتِ فيها لتصبحي هذه الإنسانة الرائعة التي نراها اليوم. نتمنى لكِ في عامكِ الجديد أن تضحك لكِ الدنيا كما تضحكين لنا، وأن يفتح الله لكِ أبواباً من النجاح لا تُسد، وأحلاماً تتحقق بلمح البصر.
                    </p>
                    <p>
                        بارك الله في عمرك، وزادكِ جمالاً فوق جمالك، ونقاءً فوق نقائك. أدعو الله أن تكون أيامك القادمة كلها ربيعاً، وأن يحفظ لكِ تلك الابتسامة التي هي سر قوتنا وفرحنا.
                    </p>
                    <p class="font-bold text-pink-600 text-center text-2xl mt-8 italic">
                        "كل عام وأنتِ بستان الورد في عائلتنا، كل عام وأنتِ بخير يا فرح"
                    </p>
                </div>
            </div>

            <!-- الرسالة الخاصة (سهيل) -->
            <div class="glass-panel p-12 border-pink-600 bg-red-50 relative">
                <div class="text-5xl mb-6 heart-beat">❤️‍🩹</div>
                <p class="text-3xl md:text-5xl font-black text-red-800 leading-tight">
                    أتمنى ألا تنسي <span class="underline decoration-pink-300">سهيل</span><br>رغم أنني أتوقع أنكِ نسيتِه تماماً..
                </p>
                <p class="mt-6 text-gray-500 italic">ستبقين دائماً في الذاكرة</p>
            </div>

            <div class="text-pink-400 opacity-60 text-sm">تم صنع هذا الموقع خصيصاً بمناسبة عيد ميلادك الـ 19</div>
        </div>
    </div>

    <script>
        function checkPassword() {
            const pass = document.getElementById('password').value.trim().toLowerCase();
            if (pass === 'farah') {
                document.getElementById('login-section').style.display = 'none';
                document.getElementById('celebration-section').classList.remove('hidden-content');
                startBalloons();
                window.scrollTo(0, 0);
            } else { 
                document.getElementById('error-msg').classList.remove('hidden'); 
            }
        }

        // إعداد الخلفية المتحركة
        let scene, camera, renderer, objects = [];
        function init3D() {
            scene = new THREE.Scene();
            camera = new THREE.PerspectiveCamera(75, window.innerWidth/window.innerHeight, 0.1, 1000);
            renderer = new THREE.WebGLRenderer({ canvas: document.getElementById('bg-canvas'), alpha: true, antialias: true });
            renderer.setSize(window.innerWidth, window.innerHeight);
            const light = new THREE.DirectionalLight(0xffffff, 1.2);
            light.position.set(1, 1, 5);
            scene.add(light);
            scene.add(new THREE.AmbientLight(0xffffff, 0.8));

            const geometry = new THREE.SphereGeometry(0.18, 24, 24);
            for (let i = 0; i < 50; i++) {
                const material = new THREE.MeshPhongMaterial({ 
                    color: i%3===0 ? 0xff4d6d : (i%3===1 ? 0xffb3c1 : 0xff758f),
                    shininess: 100
                });
                const sphere = new THREE.Mesh(geometry, material);
                sphere.position.set((Math.random()-0.5)*18, (Math.random()-0.5)*18, (Math.random()-0.5)*18);
                sphere.userData.speed = Math.random() * 0.008 + 0.003;
                scene.add(sphere); objects.push(sphere);
            }
            camera.position.z = 10;
            
            function animate() {
                requestAnimationFrame(animate);
                objects.forEach(obj => { 
                    obj.position.y += obj.userData.speed; 
                    obj.rotation.x += 0.01;
                    if(obj.position.y > 10) obj.position.y = -10; 
                });
                renderer.render(scene, camera);
            }
            animate();
        }

        function startBalloons() {
            const emojis = ['🎈', '🌸', '✨', '💖', '🎂'];
            for(let i=0; i<40; i++) {
                setTimeout(() => {
                    const b = document.createElement('div');
                    b.className = 'balloon';
                    b.innerHTML = emojis[Math.floor(Math.random() * emojis.length)];
                    b.style.left = Math.random() * 95 + 'vw';
                    b.style.top = '110vh';
                    b.style.fontSize = (Math.random() * 20 + 25) + 'px';
                    b.style.transition = `top ${Math.random()*6 + 6}s linear`;
                    document.body.appendChild(b);
                    setTimeout(() => b.style.top = '-15vh', 100);
                    setTimeout(() => b.remove(), 12000);
                }, i * 300);
            }
        }

        window.onload = init3D;
        window.addEventListener('resize', () => {
            camera.aspect = window.innerWidth/window.innerHeight;
            camera.updateProjectionMatrix();
            renderer.setSize(window.innerWidth, window.innerHeight);
        });
    </script>
</body>
</html>

